[pytest]
norecursedirs = tmp.* build benchmark _site OLD
addopts = -p no:warnings
testpaths =
    tests/basic
    tests/help
    tests/browser
    tests/scrape

markers =
    network: marks tests that require network or paid API access (skipped unless --run-network)

env =
    AIDER_ANALYTICS=false

